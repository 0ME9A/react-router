import React from 'react';
import { Outlet } from 'react-router-dom';

function Home(props) {
    return (
        <div>
            <h1>This is home page...</h1>
        </div>
    );
}

export default Home;